function openAddClass(form) {
	// Class added for professor
	document.getElementById('navigationButtons').action = "AddClass_Professor.html";
}

function openSendMessage(form) {
	// Button to send message
	document.getElementById('navigationButtons').action = "SendMessage.html";
}